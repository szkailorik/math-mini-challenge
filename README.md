# Mini Challenge · 专家私教引擎 v23.69

小学数学高密度训练单页应用：基于遗忘曲线的智能出题 + 永久错题本 + GitHub Gist 多设备同步。

## v23.69 本套报告变式题质量增强

- 这次继续加强 `本套错题变式跟训` 的题目质量，不再只停留在“能生成”。
- 重点提升了这几类变式的命中感：
  - 分数运算
  - 互化与比较
  - 单位 / 比率 / 速度
  - 第二阶段 `c2_*` 相关题
- 现在这块会更优先复用系统里本来就有的高质量变式库和边界题，而不是过早退回通用题。
- 目标就是让变式题更像“在补同一个错误机制”，不是泛泛换一道题。

## v23.68 本套报告变式跟训分组优化

- 这次继续优化 `本套错题变式跟训` 的页面结构和训练顺序。
- 我把题目从一长串列表，改成了按错误点家族分组的小块：
  - 先补概念错
  - 再做粗心保温
- 现在家长和孩子会更容易看出“先练哪一类”，打印出来也更像一份真正的补练内页。

## v23.67 本套报告错题变式跟训

- 这次在 `本套报告 / Set Review` 里新增了“本套错题变式跟训”板块。
- 系统会基于本套真实错题，按错误点家族生成一轮短而精的同类变式训练。
- 这不是原题重做，而是保持同知识点、同方法、但更换数字、题面和答案。
- 这一步同时新增了：
  - `打印变式训练`
  - `打印训练 + 答案`
- 这样家长批改完本套以后，就可以直接在报告里继续做一轮针对性补练。

## v23.66 计算知识页打印讲义优化

- 这次继续优化 `计算知识总览 / Quick Review`，重点转到打印讲义版式。
- 我把打印态下的页头、焦点卡、导航、专题卡、三步主线、补充区和例子区都继续统一成更克制的讲义样式。
- 这次不是改知识点，而是让打印出来更像老师可以直接发的小讲义。
- 这一步主要改善的是：
  - 打印版更整齐
  - 卡片和分区更统一
  - 纸面更像讲义，不像网页截图

## v23.65 计算知识页内容提纯

- 这次继续优化 `计算知识总览 / Quick Review`，重点不再是版式，而是文案本身。
- 我把 8 个专题里的 `先记住 / 先做这一步 / 最后检查 / 规则 / 技巧 / 易错` 一起压短了一轮。
- 这次的标准是：
  - 更短
  - 更像口令
  - 更像孩子做题前真的会用到的话
- 这一步主要改善的是：
  - 更容易快速想起来
  - 更适合练前、练后和考前速看
  - 页面更像“唤醒记忆”而不是“解释知识”

## v23.64 计算知识页专题卡再收顺

- 这次继续优化 `计算知识总览 / Quick Review` 的专题正文。
- 我把每个专题块进一步收成更像高质量速查卡的结构：
  - 上面先抓 `先记住 + 回想顺序`
  - 中间是三步主线
  - 下面再看补充速查和例子
- 同时把专题块内部的 spacing、字号和块感都继续压顺了一层。
- 这一步主要改善的是：
  - 专题更好扫读
  - 逻辑更顺
  - 打印出来也更像一张真正的复习速查卡

## v23.63 计算知识页页头与焦点区收轻

- 这次继续优化 `计算知识总览 / Quick Review`。
- 我把页头改成了更轻的两栏封面结构，同时把 `今日先看` 两块焦点卡压得更紧凑。
- 这一步没有继续加知识点，而是让孩子打开后更快进入真正的专题主体。
- 这一步主要改善的是：
  - 页头更省空间
  - 焦点区更容易扫一眼
  - 打印时也更像一张克制的复习讲义首页

## v23.62 悬浮菜单状态层轻量化

- 这次继续优化右上角 `专家私教引擎` 悬浮菜单。
- 我把阶段状态区从原来还比较像一张独立卡片的样子，改成了贴在切换区下面的一条更轻的状态层。
- 同时把 phase chip、status chip 和 insight 标签都再压细了一层。
- 这一步主要改善的是：
  - 菜单块感更轻
  - 顶部看起来更像紧凑工具条
  - 同样的信息占更少高度

## v23.61 悬浮菜单继续瘦身

- 这次继续优化右上角 `专家私教引擎` 悬浮菜单。
- 我把当前 program 的冗余标题区隐藏了，因为上面的阶段切换按钮和 phase chip 已经足够表达当前所在阶段。
- 同时把菜单宽度、卡片 padding、状态标签和按钮间距又压短了一层。
- 这一步主要改善的是：
  - 菜单再短一点
  - 顶部结构更紧凑
  - 常用按钮更容易留在首屏里

## v23.60 悬浮菜单动作区再压缩

- 这次继续优化右上角 `专家私教引擎` 悬浮菜单。
- 我把下半部分常用入口从一长列，改成了更紧凑的双列动作区。
- 同时把云端状态也收成了更短的一条状态条，减少顶部占高。
- 这一步主要改善的是：
  - 常用入口更容易一屏看到
  - 菜单整体更短
  - 下面那几项不容易再被挤到屏幕外面

## v23.59 悬浮菜单压缩与短屏修正

- 这次优先优化的是右上角 `专家私教引擎` 悬浮菜单。
- 我把第一阶段 / 第二阶段那几段介绍性长文案从菜单里去掉了，只保留真正有用的切换、状态和 insight 标签。
- 整个悬浮菜单也一起压短，并增加了短屏高度保护和内部滚动。
- 这一步主要改善的是：
  - 菜单不再一直向下挤
  - 底部按钮更容易找到
  - 小屏幕或窗口较矮时也不会把下面的功能挤没

## v23.58 计算知识总览页讲义感深化

- `计算知识总览 / Quick Review` 这一轮继续往“高质量讲义”方向收。
- 我把 `当前更值得多看` 收成了一句更清楚的话，不再像很多小标签并列。
- 三步主线现在也更像真正的顺序步骤：
  - `1 先做这一步`
  - `2 最容易错`
  - `3 最后检查`
- 同时把补充区继续压轻，整个页面看起来会更安静、更容易扫读。

## v23.57 计算知识总览页逻辑与版式收束

- `计算知识总览 / Quick Review` 这一轮继续围绕“逻辑更顺、页面不乱”做了收束。
- 每个专题现在统一按：
  - 一句总纲
  - 一条做题主线
  - 一块轻量补充
 这种结构来读。
- 我也把颜色和块感压克制了一层：
  - 导航更像目录
  - 焦点卡更简洁
  - 专题区更像讲义
- 这一步的重点不是加知识，而是让孩子更快知道先看什么、怎么往下看。

## v23.56 计算知识总览页结构重排

- `计算知识总览 / Quick Review` 这一轮不再继续加提示块，而是把每个专题真正重排成了：
  - 一句总纲
  - 一条做题主线
  - 一块轻量补充
- `先做这一步 / 最容易错 / 最后检查` 现在不再是很多分散提醒，而是收成一条更顺的做题主线。
- `核心规则 / 计算技巧 / 典型例子` 现在也压成了更轻的补充区，不再和主线抢注意力。
- 整体看起来会更像高质量讲义，而不是很多并列卡片拼在一起。

## v23.55 计算知识总览页收尾自检升级

- `计算知识总览 / Quick Review` 里的每个专题现在都新增了 `最后检查` 一句自检提示。
- 现在孩子打开一个专题时，会先看到：
  - `先记住`
  - `最容易错`
  - `先做这一步`
  - `最后检查`
- 也就是说，这页现在已经把一个计算题的复习闭环收得更完整了：起手、避坑、执行、收尾。
- 这对练后快速复盘和考前快速稳手感都更有帮助。

## v23.54 计算知识总览页起手动作升级

- `计算知识总览 / Quick Review` 里的每个专题现在都新增了 `先做这一步` 动作提示。
- 现在孩子打开一个专题时，会先看到：
  - `先记住`
  - `最容易错`
  - `先做这一步`
- 也就是说，这页现在不只是提醒规则和错误，还会直接告诉孩子“看到这种题，第一步先干什么”。
- 这对练前快速进入状态尤其有帮助。

## v23.53 计算知识总览页防错提醒升级

- `计算知识总览 / Quick Review` 里的每个专题现在都新增了 `最容易错` 一句提醒。
- 这一步还是没有去堆知识点，而是把最常见的那个坑先提到前面。
- 现在孩子打开一个专题时，可以先抓：
  - `先记住`
  - `最容易错`
  - 再看回想顺序、规则、技巧、例子和易错点
- 这样更适合练前快速防错，也更适合练后对照复盘。

## v23.52 计算知识总览页记忆钉子句升级

- `计算知识总览 / Quick Review` 里的每个专题现在都新增了 `先记住` 一句核心提醒。
- 这一步不是继续加知识点，而是把最值得先想起来的那一句直接提到最前面。
- 现在孩子可以先抓：
  - 一句核心提醒
  - 再看回想顺序
  - 再看规则、技巧、例子和易错点
- 这样更像真正的高效复习页，而不是一堆并列的信息块。

## v23.51 计算知识总览页焦点引导升级

- `计算知识总览 / Quick Review` 现在新增了更醒目的 `今日先看` 焦点区，会把当前最值得先复习的两个专题直接提到页面上方。
- 这次不是继续堆知识点，而是把“今天先看什么”做得更清楚，让小朋友更快进入状态。
- 总览导航现在可以直接 `直达这个专题`，更适合快速扫读，也更适合练前和练后短时间复习。
- 打印态也同步保留了这两个焦点专题的紧凑摘要，让讲义第一页更容易抓重点。

## v23.50 计算知识总览页二次打磨

- 在 `计算知识总览 / Quick Review` 里新增了“什么时候看、先看什么、每个专题怎么扫”的复习引导。
- 顶部现在会明确提示：
  - 练前 30 秒怎么用
  - 练后 1 分钟怎么用
  - 考前 2 分钟怎么用
- 每个专题现在都补了 `回想顺序`，帮助孩子更快进入正确的扫描节奏，而不是只看到一堆知识块。
- 总览索引也变成了带编号的扫描导航，打印出来更像一份真正可执行的复习讲义。

## v23.49 计算知识总览页上线

- 新增 `📘 计算知识总览 / Quick Review` 入口，面向小朋友快速回想关键计算规则、技巧、典型例子和易错提醒。
- 页面结构采用 `总览 + 分专题展开 + 底部速查钉子句`，不是知识堆砌页，而是和日常训练配合使用的“知识唤醒页”。
- 局部会根据当前阶段和活跃弱项给出“当前更值得多看”的轻提示，但主内容保持稳定，便于长期反复看。
- 页面专门做了屏幕阅读和打印样式，可直接打印成 1 到 2 页的高质量复习讲义。

手动验收建议：
- 打开 `计算知识总览`
- 检查 8 个专题是否完整出现
- 切换第一阶段 / 第二阶段，确认主内容不变，只变推荐与强调句
- 点击打印，确认控制按钮不进入打印页，讲义结构完整

## v23.48 旧知稳态边界强化升级

- 这次继续深化的是第二阶段 `Closure` 的 `stabilityGap L3`。
- 现在“旧知稳态”这一类题，在进入 `L3` 时也不再只是普通同类变式，而会优先走真正的 `boundary` 强化题。
- 这类题现在更强调：
  - 先判断商应大于 1 还是小于 1
  - 先判断小数点该落在哪个区间
  - 再去做精确计算
- 也就是说，第二阶段现在在四个高价值方向上都已经有了更真实的 `L3 boundary`：
  - `methodGap`
  - `validationGap`
  - `speedGap`
  - `stabilityGap`

## v23.47 速度稳定边界强化升级

- 这次继续深化的是第二阶段 `Closure` 的 `speedGap L3`。
- 现在“速度稳定”这一类题，在进入 `L3` 时也不再只是普通同类变式，而会优先走真正的 `boundary` 强化题。
- 这类题现在更强调：
  - 先识别结构再决定快算法
  - 先判断数量级和商的大致范围
  - 再去确认哪种算法最快、最稳
- 也就是说，第二阶段现在在三个高价值方向上都已经有了更真实的 `L3 boundary`：
  - `methodGap`
  - `validationGap`
  - `speedGap`

## v23.46 结果检验边界强化升级

- 这次继续深化的是第二阶段 `Closure` 的 `validationGap L3`。
- 现在“结果检验”这一类题，在进入 `L3` 时也不再只是普通同类变式，而会优先走真正的 `boundary` 检验题。
- 这类题现在更强调：
  - 先看数量级
  - 先看结果大概落在哪个范围
  - 再决定一个答案是不是可信
- 也就是说，第二阶段现在在两个高价值方向上都已经有了更真实的 `L3 boundary`：
  - `methodGap`
  - `validationGap`

## v23.45 第二阶段边界强化升级

- 这次继续深化的是第二阶段 `Closure` 的 `L3` 强化题质量。
- 之前 `L3` 已经有层级语义了，但有些题本质上还是普通同类变式。现在 `methodGap` 里的重点题会优先走真正的 `boundary` 强化题，不再只是“普通变式 + L3 标签”。
- 第一批已经专门升级的是这些第二阶段方法类题：
  - `c2_eq_percent`
  - `c2_rate_discount`
  - 以及部分单位换算题
- 这类题现在更像“边界检验题”：
  - 先判断部分量/整体量
  - 先识别剩余百分率
  - 先统一量纲再计算
- 这一步的重点不是让题更难看，而是让第二阶段真的能检验“方法选择和迁移”。

## v23.44 共享质量家族扩面

- 这次把共享质量家族继续往前扩了一轮，不再只覆盖 Lorik 分数题和少数桥接题。
- 新接入的关键范围是：
  - `KAI` 的分数计算题 `k_fcalc_*`
  - 第二阶段的方程题 `c2_eq_*`
- 这意味着这两类题现在也会正式吃到这套共同训练质量内核：
  - `qualityFamily`
  - `replayLevel`
  - `explanationMode`
- 第一阶段最直接的变化是：KAI 的分数计算题现在也能进入 `高价值回炉位`，不再只有小数除法和互化题更容易被精准追打。
- 第二阶段最直接的变化是：方程题现在会拿到结构化讲解模式，不再只是普通步骤文本。

## v23.43 共享讲解模式上线

- 这次把共享训练质量内核继续往答案页和复盘层推进了：首批高价值题族现在会自动带上结构化讲解模式。
- 当前已经接入的讲解模式是：
  - `规则提醒`
  - `方法提醒`
  - `检验提醒`
- 第一批接入题族仍然是这三组：
  - `小数除法`
  - `分数运算`
  - `互化与比较 / 跨表示桥接`
- 这次不是额外堆一层 UI，而是让题目对象自己带 `explanationMode`，再把原来的 `step` 统一包装成更稳定的训练语言，所以正常题、错题回炉、第二阶段重点强化题都会一起受益。

## v23.42 第二阶段强化区 L1-L3 回炉

- 第二阶段 `Closure` 的第五大题现在不再只是“原题优先，撞车就换普通变式”，而是显式按 `L1 / L2 / L3` 回炉层级来选题。
- 现在的强化规则是：
  - `L1` 优先回原题，确认有没有真正记住
  - `L2` 优先出同类变式，检查会不会迁移
  - `L3` 优先出更强的边界/重点变式，检查是不是真的理解了
- 这次没有打散第二阶段现有的六大区骨架和打印结构，只升级了 `重点强化区` 的回炉选择器。
- 运行时校验也一起收紧了：现在会明确检查第二阶段强化题是否带有 `isClosureFocusReplay`、`qualityFamily` 和正确的 `replayLevel`。

## 在线访问

**https://szkailorik.github.io/math-mini-challenge/**

## 规划文档

- [下一阶段计算训练模块开发文档](./docs/2026-04-18-next-stage-calculation-spec.md)
- [第二阶段实施计划](./docs/2026-04-18-next-stage-calculation-implementation-plan.md)
- [第二阶段前端体验设计文档](./docs/2026-04-18-phase-two-frontend-ux-spec.md)
- [两个阶段共同训练质量优化设计文档](./docs/2026-04-19-shared-training-quality-spec.md)
- [两个阶段共同训练质量优化实施计划](./docs/2026-04-19-shared-training-quality-implementation-plan.md)
- [计算知识总览页设计文档](./docs/superpowers/specs/2026-04-19-calculation-quick-review-design.md)
- [计算知识总览页实施计划](./docs/superpowers/plans/2026-04-19-calculation-quick-review-implementation-plan.md)

## 多设备同步设置（一次，30 秒）

1. 打开 https://github.com/settings/tokens/new?scopes=gist&description=math-mini-challenge （已预勾 gist 权限）
2. Expiration 选 **No expiration**，点底部 **Generate token**
3. 复制 token（形如 `ghp_xxxx...`）
4. 在应用右上角控制面板点 **💾 数据备份** → 粘贴 token → **🔗 连接云同步**
5. 换设备（iPad/电脑/其他浏览器）时粘贴同一 token → 数据自动合并同步

连接云同步后，数据存在你自己 GitHub 账号下一个**私有 gist** 里（描述为 `math-mini-challenge-sync-v1`）。GitHub Pages 只是网页入口，不保存学习记录。随时可在 https://gist.github.com 查看/删除/导出。不走任何第三方服务器，Anthropic / 其他云服务商看不到你的数据。

## v22.0 重点：错题本永久存档版

相比 v21.0，重写了错题本存储与交互：

| 维度 | v21 旧版 | v22 新版 |
| --- | --- | --- |
| 存储位置 | 挂在 `history[]` 的 session details 里 | 独立 `errorBook{}` 字典，单独持久化 |
| 超 60 次训练后 | `history.shift()` 会裁剪掉老错题 | errorBook 不裁剪，永久保留 |
| 同一题错多次 | 重复出现多张卡片 | 自动去重，合并为一条 + `count` 计数 |
| 重新提交同 set | 错题会被覆盖/丢失 | 可回滚的增量计数，历史错次不丢 |
| 已掌握的题 | 无法从错题本移除 | ✓ 按钮标记掌握，单独归档；再次错→自动撤销 |
| 知识点视图 | 时间线混排 | 按 KnowledgeBase tag 自动分组，组内按错次排序 |
| 数据迁移 | — | 从旧版 `history` 自动重建 errorBook |

核心错题唯一 ID：`tag + FNV-1a(normalized(q))`，不同题不会碰撞，同题跨 set 稳定命中。

## v23.41 两页内单题单行排版

- 这一轮把约束收成整套卷子的全局约束：所有题都必须继续压在两页里，同时每一道题都必须完整占一行。
- 现在不再用“分数题单列保完整”的办法，而是改成：
  - 题块整体降高
  - 分数区恢复紧凑多列
  - section 列数按题目复杂度在 2 列 / 3 列之间自适应
  - 单题容器统一锁成单行，不再允许一道题内部断开错位

## v23.40 分数题区改为单列

- 这一轮继续把“分数那部分，一道题必须完整排在一行”往前推。
- 这次不是只收间距，而是直接把分数题区改成单列排布：
  - 一题一行
  - 不再和旁边另一题并排抢横向空间
  - 宁可这一整块更高，也不让单题在内部断开

## v23.39 分数题单题单行优先

- 这一轮专门按“分数那部分，一道题必须完整排在一行”来修。
- 现在分数题区不再硬走原来的三列，而是改成更宽的列数配置，并把这一块的行距和列间距一起收紧。
- 目标很明确：
  - 单题完整
  - 不错位
  - 宁可整块往下长，也不让一道题自己断开

## v23.38 等号颜色回归正文色

- 这一轮把等号从红色改回和题目正文一致的颜色，不再额外强调。
- 我也顺手继续收紧了分数题、方程题和等号后留白的行内间距，让卷面更稳、更像正式试卷。

## v23.37 去掉等号后答案横线

- 这一轮按新要求把所有“等号后面的写答案横线”直接去掉了。
- 现在 `= ____` 这种形式会变成：
  - 保留等号
  - 去掉画出来的横线
  - 只留下很轻的写字留白，不再用下划线撑版
- 这样做的目的就是减少横线本身对排版的干扰，让分数题、互化题、方程题更容易排整齐。

## v23.36 答案横线长度收紧

- 这一轮只修一个卷面问题：答案横线太长。
- 现在把题面里的答题横线整体收短了一档，避免题目后面拖出过长留白，让卷面更像正常试卷里的作答位。
- 打印态也同步收短了，所以不会出现屏幕和纸面长度不一致。

## v23.35 恢复旧版题面行内规则

- 这一次我没有继续给当前规则叠补丁，而是直接把题面渲染拉回更接近旧版试卷的方式。
- 核心变化是：
  - 撤掉最近那套“独立答题尾部组件”
  - 回到 `题干 + 等号 + 空格线 + 后缀` 的单一行内规则
  - 让分数、等号、空格线、百分号、单位重新按同一行节奏排版
- 这次的目的不是做新样式，而是尽量靠回你截图里那种更稳、更像纸面试卷的题面骨架。

## v23.34 等号与空格线统一修正

- 这一轮继续专门修“等号重复、等号跑到奇怪地方、空格线乱飞”的问题。
- 这次我把一整类最容易出错的题统一收口了：
  - 原来很多题本身是 `= 空格线`
  - 后面系统又有一套统一尾部答题规则
  - 两套逻辑叠在一起时，就容易出现等号位置奇怪、空格线像多出来一条的感觉
- 现在这类题统一改成共享的尾部结构：
  - `题干`
  - `= ____`
  - 必要时再跟 `% / 单位 / 其他尾部文本`
- 这样像 `9/25 = ____ %`、`x = ____`、`0.72 km = ____ m` 这种题，等号和空格线会更像正式试卷里的同一套规则。

## v23.33 分数题试卷密排修正

- 这一轮继续处理“逻辑已经补对，但分数题在卷面三列里还是容易挤坏”的问题。
- 现在对分数重题和混合运算题又做了一层更像试卷的密排：
  - 分数/括号题整行字体再压一档
  - 分数块、括号、答题槽都进一步缩紧
  - `= ____` 尾部跟算式之间的空隙继续收窄
  - 打印态同步走更紧的密排版本
- 这一轮重点不是改出题逻辑，而是让已经修好的等号和答题尾部更容易真正留在同一行里。

## v23.32 试卷行内算式排版修正

- 这一轮专门继续修“很多题还没有等于号、很多分数题没有排在一行”的问题。
- 这次真正找到的根因是：
  - 上一版判断“题干里是否已经有等号”时，误把 HTML 里的 `class="frac"` 这类属性也当成了等号
  - 所以很多分数运算题被错误识别成“已经有等式”，尾部 `= ____` 就被吞掉了
- 现在这轮把这个判断改成了“只看真正可见的数学文本”，不再把 HTML 属性算进去。
- 同时我把分数题和括号题进一步收成“试卷行内算式”：
  - 分数/括号题会优先走单行 exam-row 排版
  - `= ____` 尾部跟着整行算式走，不再像网页流式文本那样容易散开
  - 打印态也同步按更紧的单行节奏收过一轮

## v23.31 数学排版结构化修正

- 这一轮不是继续微调一点点样式，而是把题目渲染骨架重新收稳了。
- 之前分数题和部分填空题会乱，核心原因是题干里混进了块级标签：
  - 分数还是 `<div class="frac">`
  - 填空横线还是 `<div class="blank">`
  - 再叠加旧的“靠题干里有没有 `=` 猜后缀等号”逻辑，就很容易出现横线乱跑、等号不统一、括号和分数线不在一个节奏上。
- 现在这轮改成了更稳定的结构：
  - 先把分数和横线统一规范成行内元素
  - 再把“题干内容”和“尾部答题区”拆开渲染
  - 对于结尾求值题统一走共享的 `= ____` 答题尾部
  - 对于比较题、圈符号题、内部本来就有等式的题，不再额外硬补尾部等号
- 分数括号题也加了专门的行内分组，减少括号、分数线、等号互相打架。
- 打印态同步做了更轻的缩放，避免这轮修复只在屏幕上好看，打印时又重新挤坏。

## v23.30 题目显示与打印对齐修正

- 这一轮专门修了题目“显示不全、排列和序号错位”的问题。
- 根因是新版 booklet 样式增加了 section 标识、meta band 和页脚，但题块还在用固定高度，导致长题在屏幕和打印里都可能被硬裁掉。
- 现在的修法是两层：
  - 屏幕上：题块从固定 `height` 改成 `min-height`，优先保证题目完整显示
  - 打印时：单独压缩字号、间距、meta band 和题块高度，继续守住四页分页
- 同时把题号结构拆成独立列，不再让序号跟题干一起换行，所以长题时序号会更稳。

## v23.29 Answer Key Booklet 对齐

- 这一轮继续完善“整本册子”的一致性，重点落在答案页和页脚。
- 现在题卷页和答案页已经统一进同一套 booklet 语言：
  - 每页都有统一的页脚信息
  - 答案页也有自己的 meta band
  - 解析区的大题标题更清楚，更像正式 teacher key / solutions booklet
- 这次没有改训练逻辑，只继续优化老师拿在手里的整套打印材料。

## v23.28 Assessment Booklet 试卷深化

- 这一轮继续只打磨学生题卷和打印卷，不改训练主逻辑。
- 题卷现在进一步靠近正式 assessment booklet：
  - 每个大题都有更明确的 section 标识
  - 每页题卷都有轻量的 meta band，直接告诉老师和学生这页属于哪个 program / 哪张卷 / 当前重点
  - 题目留白、行距、section 节奏又收紧了一轮，读题更像正式试卷
- 这次仍然坚持分层：
  - 上面 `no-print` 是老师工作台
  - 下面学生页和打印页继续保持考试卷气质

## v23.27 手动切阶段与考试卷工作台

- 一阶段和二阶段现在不再由系统判断或拦截，而是完全由老师 / 监护人手动切换。
- 顶部控制区不再是“晋级判断卡”，而是更清楚的老师工作台：
  - 直接点 `第一阶段`
  - 直接点 `第二阶段`
  - 系统不会再弹 gate，也不会再要求 `继续巩固 7 套`
- 底层数据仍然保持联通：
  - 两个阶段继续各自记自己的套号和训练画像
  - 第一次切到第二阶段时，会自动把第一阶段的关键信号接过去
  - 所以切换阶段不是进两个完全割裂的系统
- 这轮还把题目页进一步收成更像考试卷，方向更靠近 `新加坡数学` 常见练习册 / assessment booklet：
  - 更干净的页头
  - 更规范的 section 分隔
  - 更克制的颜色和边线
  - 更强调题干、空格、分数线、括号、单位和比较符的规范感
- 也就是说，现在整体体验是分层的：
  - 上面是老师工作台
  - 下面学生看到和打印出来的是考试卷页面

## v23.26 第二阶段重点强化区回炉

- 第二阶段 `Closure` 现在又往前走了一步：第五大题不仅会先判断“今天最该强化哪类 gap”，还会优先把这类 gap 里的活跃错题塞回当天训练。
- 回炉顺序是：
  - 先尝试回原题
  - 如果原题和当前卷子撞车，就退到同类变式
- 这次我特意没有把整张卷子做成到处回炉，而是把回炉聚焦在 `重点强化区`，这样有几个好处：
  - 孩子会明显感觉到“今天就是在追打这类问题”
  - 卷子整体骨架和打印排版不会乱
  - 第二阶段的 `总收束 + 旧知保温` 主线不会被局部补漏拖散
- 现在第二阶段第五大题已经是三层联动了：
  - 先看当前阶段
  - 再看当前最突出的 gap
  - 最后把这个 gap 里的错题原题或变式真正带回卷子

## v23.25 第二阶段 gap 自适应强化

- 第二阶段 `Closure` 现在不只会按 `收口期 / 收束期 / 毕业判定期` 递进，还会根据每个孩子当前最突出的 second-stage gap 来动态改卷。
- 这次没有把整张卷子打散，而是把第五大题升级成了 learner-specific 的 `重点强化区`。
- 系统现在会综合这几个来源判断当天最该强化什么：
  - 第二阶段 5 类 gap 信号
  - 第二阶段活跃错题本
  - 最近几套的错误轨迹
  - 当前所处阶段的训练偏置
- 所以 KAI 和 Lorik 现在可能在同一套 `Closure` 里看到不同的第五大题重点，例如：
  - `重点强化：跨表示桥接`
  - `重点强化：单位与建模`
  - `重点强化：旧知稳态`
  - `重点强化：速度稳定`
  - `重点强化：结果检验`
- 这样第二阶段不再只是“统一综合收束”，而是开始真正做到“同一训练骨架下的个性化高价值追打”。

## v23.24 第二阶段三段式收束

- 第二阶段 `Closure` 现在不再只是同一套综合卷反复出，而是按一个月训练过程拆成了 3 个阶段：
  - `收口期`
  - `收束期`
  - `毕业判定期`
- 三个阶段不是只换标题，题卷内容也会随阶段移动重点：
  - `收口期` 更强调统一表示、基础量纲和先估再算
  - `收束期` 更强调综合迁移、方法选择和跨题型切换
  - `毕业判定期` 更强调稳态抽检、反向表达和结果校验
- 第二阶段仍然坚持 `总收束 + 旧知保温`：
  - 新阶段继续综合百分数 / 分数 / 比 / 单位 / 方程 / 估算 / 速度
  - 同时每天继续保留第一阶段最容易回落的除法、乘法、减法手感
- 这一轮还把第二阶段状态卡、答案页和题卷引导文案一起改成跟阶段同步，方便直接判断当前这一套是在练什么。

## v23.23 第二阶段晋级闭环

- 这是 `v23.27` 之前的一版历史过渡方案，已经被“老师手动直接切阶段”的工作台取代。
- 当时留下来、现在仍然保留的底层收益主要有两项：
  - 第一阶段和第二阶段各自保持自己的 set counter
  - 第一次进入第二阶段时会做一次轻量 bootstrap，把第一阶段的高价值弱域折算进第二阶段的起始关注点

## v23.22 第二阶段闭环上线

- 第二阶段 `小学计算收束阶段` 现在已经从“可出卷可打印”推进到了真正的训练闭环。
- 这一版的定位还是一样：不是简单拔高，也不是托底补漏，而是站在六年级到初中的视角，把小学阶段最有价值的计算能力做成“**总收束 + 旧知保温**”的组合训练。
- 题卷结构会同时覆盖：
  - 百分数 / 分数 / 比的桥接互化
  - 单位换算与量纲统一
  - 百分率、折扣与方程
  - 估算、速度与结果判断
  - 第一阶段核心竖式与乘减能力保温
- `打印AB四页` 和 `打印答案` 继续能直接用于第二阶段，不会影响第一阶段现有打印主链。
- 第二阶段现在已经开放：
  - 题卷生成
  - 答案页批改
  - 本套报告
  - 独立画像沉淀
- 第二阶段画像会单独记录 5 类 gap 信号：
  - `representationGap`
  - `methodGap`
  - `stabilityGap`
  - `speedGap`
  - `validationGap`
- 这些信号写在第二阶段自己的 program 画像里，不会反向污染第一阶段。

## v23.19 数学排版美化

- 这一轮重点不是题库，而是把整张卷子的数学排版做得更稳、更匀、更像正式试卷。
- 统一优化了题号、分数线、运算符、等号、比较符、空格线、圆圈填空和循环小数点这些位置的视觉表现。
- 题目里的 `× ÷ − = < >` 会更清楚地站出来，不会再有有些地方显得轻、有些地方显得挤的感觉。
- 分数、括号和带分数的间距也重新调了，打印时会更平衡，读题更顺。
- 这次同时兼顾了屏幕查看和打印质感，不是只做浏览器里好看。

## v23.18 高价值训练强化

- 这一轮不只是继续加题，而是把“哪些题最值得优先练”正式写进了出题引擎。
- 系统现在会更主动地把训练时间集中到高价值弱项上，特别是：
  - 小数除法里最容易假会的移位、小数商、验算链
  - 方程里最容易顺手做错的减数、除数、两步逆运算
  - 分数连除连乘、交叉约分
  - 互化与比较里的高频基准辨析
- 同时我把 KAI 这边的高价值错题回炉也升级了，不再只靠原题回炉，而是会出现更像真正巩固训练的同类变式。
- 目标是让整张卷更“会抓重点”，不是平均用力。

## v23.17 B卷除法提质

- 这一轮继续往“训练质量”本身推进，重点提升了 Lorik `B卷第二大题 / 进阶竖式除法` 的诊断含金量。
- 现在这一区不只是继续覆盖“被除数有小数点 / 除数有小数点 / 两边都有小数点”，还更稳定地练到：
  - 商小于 1 和商大于 1 两种判断
  - 需要同乘 `10` 和同乘 `100` 的移位情况
  - 被除数或除数含两位小数的题
  - 估商、点小数点、再用乘法验算这条完整链路
- 整数除法骨架也轻微提难，不再偏保守的小范围数字。
- 错题回炉里的同类变式除法题同步升级到同一标准，避免平时训练和复习题强度脱节。
- 套卷缓存升级，后续新生成的卷子会直接使用这批新的 B 卷除法题。

## v23.16 打印稳定修正

- 这一轮把 `打印AB四页` 和 `打印答案` 改成了“独立打印舞台”模式，不再直接从当前长页面硬切打印。
- 点击打印后，系统会先把要打印的题卷或答案页复制到隐藏的专用打印容器，再只让这个容器进入浏览器打印。
- 题卷打印恢复为更刚性的 A4 物理页输出，并把打印页高微调到略低于 `297mm`，专门规避浏览器把每张题卷算成“略高于一页”后额外插空白偶数页的问题。
- 打印结束后，这个专用打印容器会自动清空，不会污染正常页面状态。

## v23.15 错题回炉升级

- 这一轮把“错题回炉”从只回原题，升级成了“原题回炉 + 同类变式回炉”。
- 目前先把最值钱的几类做进去了：Lorik 的分数小数比较题，以及 B 卷第二大题里的小数除法题。
- 也就是说，系统现在不只是把错过的题再拿出来，还会在同一个知识点上给一题更贴近边界的变式，检查孩子是不是真的会了。
- 这些回炉变式仍然会带 `复习题` 小角标，但不影响考试排版和打印分页。

## v23.14 互化辨析强化

- 这一轮重点增强了“互化与比较”区的训练质量，不是简单多做题，而是让比较题更接近真正的易错边界。
- Lorik 的分数小数比较题现在不再用过宽的随机区间，而是集中在 `0.5 / 0.625 / 0.75 / 0.8` 这些高频基准附近，让孩子必须真的比较，而不是一眼看出。
- KAI 和 Lorik 的互化题也补上了更完整的答案页步骤，这样批改后更容易复盘“为什么这么化”，而不只是看到一个结果。
- 套卷缓存再次升级，保证后续新生成的卷子直接使用这批新的辨析型题目。

## v23.13 考试排版清爽

- 题卷页现在回到更像正式考试卷的样子：去掉了题目旁边那些 `弱点 / 遗忘 / 领域 / L1-L4` 一类训练标签，也去掉了页眉里的训练状态条。
- 正常题目只保留考试式题干和原本该有的分数/括号/空格提示，不再让训练信息打断阅读。
- 只有“从错题本回炉、专门拿来复习巩固”的题，才会保留一个很轻的 `复习题` 小角标。
- 这个角标用绝对定位，不占题目行高，不改变分页，也不会把打印版面撑乱。

## v23.12 训练质量深化

- 这一轮不只是继续加题，而是直接优化“题目本身的训练价值”。
- Lorik 的 `B卷第二大题 / 进阶竖式除法` 现在把小数除法做得更像真实训练：被除数带小数、除数带小数、两边都带小数这三类里，都开始覆盖“商大于1”和“商小于1”的情形，避免只练一种顺手题。
- 一批关键基础题补上了更能复盘的步骤提示，包括 Lorik 乘法、Lorik 减法、KAI 大数减法，以及部分小数除法题；批改后不再只是看到答案，而是更容易知道“这一类题下一次该怎么做”。
- 带分数减法原来那种 `...` 占位式步骤已经去掉，改成了可读的真实借位与通分过程。
- 套卷缓存再次升级到新版本，保证新生成的卷子直接吃到这批题质优化，而不是继续沿用旧缓存。

## v23.11 训练质量重构

- 先解决你提到的老题残留问题：套卷缓存前缀已经升级，`76` 之后这些卷会按新规则重新生成，不再继续沿用旧缓存里的 B 卷第二大题。
- Lorik 的 `B卷第二大题 / 进阶竖式除法` 继续保持每套固定覆盖四类：1 题整数除法基础，1 题被除数有小数点，1 题除数有小数点，1 题被除数和除数都有小数点；而且除数带小数点的题现在也扩到了一位和两位小数两种情况。
- 这一轮我还把几块仍然偏随机的高价值区块也一起重构成“固定覆盖骨架”：Lorik 乘法、Lorik 减法、KAI 大数减法、KAI 奥数综合。
- 目标不是单纯多加题，而是让每天都更稳定地打到关键误区：位值、退位链、凑整策略、裂项相消、分数应用和反向分配。
- 运行时验证也同步升级，后面如果这些核心区块又漂回随机失衡，脚本会直接拦下来。

## v23.10 打印分页修正

- 修正了 `打印AB四页` 仍然出现 `1,3,5,7` 有内容、`2,4,6,8` 为空白页的问题。
- 打印样式不再强制每张题卷在打印时继续占满 `297mm` 的预览高度，因为这会在部分浏览器和打印机设置下被判定成“略超一页”，从而每张后面多插一张空白页。
- 现在改成“相邻题卷之间才分页”的模式：题卷本身按内容高度打印，只在下一张题卷开始前显式换页。
- 答案页也同步改成同一套分页逻辑，避免后续出现同类空白页问题。

## v23.9 分数训练覆盖增强

- KAI 的分数计算现在也改成“每套固定覆盖关键结构”，不再让 8 道计算题完全随机漂移。
- 每套都会稳定练到：分数连加减、连除连乘/括号、分配律、交叉约分，以及 4 类“小数和分数混合运算”。
- Lorik 的分数综合题现在固定覆盖 12 个核心入口：同分母加、异分母减、分数乘除、求一个数的几分之几、顺序连算、带分数加减、3 类括号题、三因数连乘，以及小数分数桥接题。
- 这样现在 KAI 和 Lorik 的“分数块”也都从“题池随机”升级成了“每天稳定打到关键误区”的训练设计。
- 运行时验证新增 KAI/Lorik 分数块覆盖检查，后续再加题型时如果破坏了训练骨架，会被直接拦下来。

## v23.8 互化与方程覆盖增强

- KAI 的互化题现在也改成“每套固定覆盖关键类型”，不再只是从大池里随机抽 8 题。
- 每套都会固定练到：百分数互化、循环小数、分母 25/40/16 基准、有限小数转分数、精确基准小数转分数。
- KAI 的方程题现在固定覆盖四类：基础逆运算、合并/两步方程、未知数在特殊位置、比例方程。
- 这样整张卷在“小数、互化、分数、方程”四个高价值区块上的训练稳定性更高。
- 运行时验证新增互化与方程覆盖检查，避免后续题池改动让这些关键能力重新随机漂移。

## v23.7 题型覆盖增强

- KAI 的“小数乘法 / 小数除法”不再只靠随机抽题，而是每套固定覆盖关键能力位点。
- 小数乘法现在每套都会覆盖：位数核对、极小数位值、凑整策略、以及整数与小数混合乘法。
- 小数除法现在每套都会覆盖：同倍扩大、除数移位、商的小数点处理、以及同尺度小数除法。
- Lorik 的基础简算现在每套固定覆盖四类结构：凑整捷算、运算顺序、分配律逆用、组合拆分。
- 运行时验证新增这些覆盖检查，避免后续改题池时把训练骨架改散。

## v23.6 备份与打印引导优化

- 统一了应用版本号来源，减少界面、Gist 元数据、导出 JSON 之间版本不一致的问题。
- 数据导出文件现在会写入当前应用版本、导出时间、导出模式和当前 gist 标识，便于后续排查恢复来源。
- 数据导入成功后会显示“来源版本”和“当前版本”，更容易判断恢复的是不是旧备份。
- `打印AB四页` 和 `打印答案` 前会先给出轻量打印提示，提醒使用纵向、100% 缩放并关闭页眉页脚。
- toast 提示统一收口，批改归档、错题掌握和打印提示都走同一套反馈机制。

## v23.5 打印与题型覆盖优化

- 修复 `打印AB四页` 时部分浏览器会把四张题卷打成八页、其中夹杂空白页的问题。
- 打印模式不再依赖短定时器清理，而是改为 `afterprint` + print media 状态回收，避免预览尚未完成就恢复页面。
- 打印样式改成“打印时最小页高 + 显式可见页分页”，降低固定 297mm 高度和全局分页叠加造成的空白页风险。
- Lorik 的 B 卷第二部分除法现在固定覆盖三类小数练习：被除数带小数点、除数带小数点、被除数和除数都带小数点。
- 运行时验证新增 Lorik 小数除法覆盖检查，以及打印 helper 生命周期检查。

## v23.4 本套批改报告

- 控制面板新增 `本套批改报告 / Set Review`，可集中查看当前 set 的批改结果。
- 每次提交 KAI 或 Lorik 批改后，会自动弹出本套报告。
- 报告按学生列出错题的分区、纸面题号、错误类型、原题、正确答案和复盘提醒，方便和纸质卷子逐题对照。
- 没有提交的学生会显示“还没有提交这套卷子的批改记录”，避免误以为数据丢失。
- 运行时验证新增本套报告检查，确保题号、原题和答案能正确显示。

## v23.3 云端连接引导

- 本地模式时，控制面板会直接显示 `连接云同步 / Connect Sync` 快捷按钮。
- 云同步状态文字可点击，会打开数据备份与 Gist 连接面板。
- 未连接时明确提示“此设备尚未连接云端”，避免误以为 GitHub Pages 会自动保存数据。

## v23.2 跨设备自动同步增强

- 明确数据位置：未连接云同步时只在当前浏览器 localStorage；连接后同步到用户 GitHub 账号下的私有 gist。
- 启动网页时自动拉取云端并与本地合并，避免换设备打开后看不到旧记录。
- 提交批改、标记掌握、导入恢复后仍会自动推送云端。
- 切回页面或页面保持打开时会自动轻量拉取，减少两台设备同时使用时的数据滞后。
- 云同步状态同时显示最近推送和最近拉取时间。

## v23.1 打印四页优化

- 控制面板新增 `打印AB四页` 按钮，点击后只把每天要做的 KAI/Lorik AB 卷四页送进浏览器打印。
- 打印模式会自动隐藏答案页、错题本页、控制面板和页面阴影，避免长页面打印时误打答案或多打空白页。
- 四张试卷标记为 `question-sheet`，第 4 页标记为 `print-last-question`，保证 A4 物理分页稳定。
- 另有 `打印答案` 按钮，方便需要单独打印答案页时使用。
- 运行时验证新增“四张可打印题卷”检查，防止后续排版改动破坏每日四页打印流。

## v23.0 错题联合训练

- 错题本正式接入日常训练卷：活跃错题会通过“原题回炉、同类变式、同领域迁移”三段方式回到训练中。
- 同一题错 2 次以上、或处于遗忘拦截阶段时，更容易触发原题回炉；每个 section 仍限制回炉数量，避免整张卷变成死刷错题。
- 同 tag 有错题但不适合原题重复时，会生成同知识点变式题，并用 `同类变式 / Variant Practice` 标记。
- 知识点地图新增“错题联合训练计划”，展示每个活跃错题将以什么方式进入训练、优先级如何。
- 试卷缓存升级到 `MathSetData_v30`，验证脚本新增错题回炉闭环检查。

## v22.9 领域自适应增强

- 出题算法新增领域级弱项信号：同一领域内的错题和权重会为相关新题提供适度加权。
- 答案页新增 `领域补强 / Domain Boost` 标记，说明这道题是为了修复某个知识领域的整体薄弱。
- 知识领域画像新增“稳定维护 / 适度加练 / 优先补强”优先级，以及活跃错次、已攻克数量。
- 试卷缓存升级到 `MathSetData_v29`，确保领域自适应评分进入新生成的 set。
- 验证脚本新增领域信号计算检查，避免知识画像只展示、不参与训练。

## v22.8 知识水准画像

- 每张训练卷新增阶段目标条，显示本轮训练目标、目标水准和检索练习/间隔/交错原则。
- 出题算法加入 L4 挑战比例控制：挑战日可以提高迁移题比例，普通日避免挑战题挤压基础巩固。
- 知识点地图升级为领域画像，按小数与位值、整数乘除与退位、分数与互化、方程与未知数、简算与综合策略汇总覆盖与弱项。
- 当前弱项表新增领域和 L1-L4 水准，方便把“错题”转成可执行的训练重点。
- 试卷缓存升级到 `MathSetData_v28`，验证脚本同步检查阶段目标条、水准标签和知识领域覆盖。

## v22.7 水准分层增强

- 出题算法加入 L1-L4 水准分层：基础、熟练、易错、挑战。
- 每个 7 套周期会按训练阶段偏向不同水准，同时保留弱项权重和遗忘曲线优先级。
- 题目和答案页都会显示水准标签，方便控制每日训练强度。
- 新增“知识点地图”，按知识族展示训练覆盖面，并标出当前活跃弱项。
- 验证脚本新增水准合法性检查，确保每道生成题都有 1-4 级训练等级。

## v22.6 反馈质量增强

- 答案页每道题新增 `[易错点 Tip]`，直接显示对应知识点和防错建议，方便批改后即时复盘。
- 新增题型家族级建议：精确标签优先，同类标签自动使用分数、互化、小数乘除、方程、简算等家族提示。
- 错题本分组与解析页也统一使用同一套知识点建议，减少无针对性的泛化反馈。
- 验证脚本现在要求每个生成题型都能拿到非泛化的知识点建议。

## v22.5 诊断训练增强

- KAI 增加两步方程、逆向分配律、单位分数应用题，强化“先逆运算、再处理乘除”和“先找单位量”的步骤意识。
- Lorik 增加括号优先级混合运算，专门拦截从左到右顺算的常见错误。
- 试卷缓存升级到 `MathSetData_v25`，确保新题型上线后能进入新生成的 set。
- 运行时验证扩展到 Set 73-102，并检查 section 数量、空题/空答案、重复题和异常字符串。

## v22.4 题型质量增强

- KAI 新增极小数乘法、小数商、未知数作除数、整百万连续借位、补偿凑整等诊断题型。
- Lorik 新增接近整百乘法、25/末尾0除法、整万连续借位、求一个数的几分之几等题型。
- 关键基础题补充解析步骤，让答案页不只给结果，也能提示孩子检查位值、估算和凑整策略。
- 互化题修正小数答案格式，统一用稳定格式输出，减少 `0.30000000004` 这类浮点显示风险。
- 新增 `scripts/validate-runtime.mjs`，可在 Node 中生成整套试卷做轻量运行时验证。

## v22.3 稳定性优化

- 修正 `k_ddiv_basic` 小数除法生成器：被除数保留 3 位，避免四舍五入后题面与答案不一致。
- 加固分数引擎：构造 `Frac` 时先把小数转换为整数比，降低浮点误差导致的异常约分风险。
- 重复提交同一套题时会回滚旧权重、应用新权重，让自适应弱点画像保持一致。
- 本地存储写入失败时会清理旧试卷快照并重试，减少 localStorage 配额满导致的保存失败。
- 避免 Lorik 异分母相减生成负数答案。
- 图片导出组件未加载完成时给出明确提示，而不是静默报错。

## v22.0-v22.2 优化

- `Frac` 构造函数：加固 `n=0` / `d=0` 边界，避免 NaN 污染
- `localStorage` 超配额：快照写入失败时自动清理其他版本缓存后重试
- 数据导入：旧版备份 (v21 及更早) 自动通过 `migrateProfile` 重建 errorBook
- 备份文件名：加日期后缀 `_2026-04-16.json` 方便版本管理
- 答题行识别：使用 `uid` 精确匹配，解决同 tag 不同题的状态串扰

## 数据结构

```
profile = {
  weights:   { tag: number },        // 掌握度加权
  lastSeen:  { tag: setNum },        // 遗忘曲线
  history:   [ {
    set, date, ts,
    details:[{tag, grade, info, uid}],         // 错题明细
    allGrades:[{tag, grade}],                  // 本套完整批改摘要
    weightAdjustments:[{tag, delta}]           // 重交批改时用于回滚画像
  } ],  // 保留 60 套
  errorBook: {                        // ★ 永久，不裁剪
    [uid]: {
      tag, info: {q,a,step,sec},
      grade: 'careless' | 'wrong',
      count: N,
      firstSet, firstDate,
      lastSet, lastDate,
      mastered: false,
      masteredDate
    }
  }
}
```

## 本地运行

单文件纯前端，无构建：

```bash
# 任选其一
python3 -m http.server 8080
npx serve .
```

浏览器访问 `http://localhost:8080/`。

## 本地验证

```bash
sed -n '/<script type="module">/,/<\/script>/p' index.html | sed '1d;$d' > /tmp/math-mini-challenge-script.mjs
node --check /tmp/math-mini-challenge-script.mjs
node scripts/validate-runtime.mjs
```

## 部署

仓库根目录的 `index.html` 直接作为 GitHub Pages 首页。workflow 在 `.github/workflows/pages.yml` 自动发布 `main` 分支的根目录。

## 浏览器要求

- Chrome / Edge / Safari 最近两个大版本
- 需要 ES Modules 支持
- 依赖 `localStorage`；云端同步使用 GitHub Gist API，需要用户提供具备 `gist` scope 的 GitHub PAT
- 图片导出依赖 CDN 加载 `html2canvas`

## 数据安全

- 默认仅本地存储（localStorage）
- 跨设备同步：GitHub Gist（PAT 存本地，不发到任何第三方服务器）
- 冲突处理：多端并发修改时按 uid/ts 安全合并，不会丢数据
- 本地始终是主副本，断网也能用，恢复网络后自动 debounce 回推
- 数据保险箱：JSON 导出/导入作为终极离线备份
