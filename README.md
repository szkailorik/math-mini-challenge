# Mini Challenge · 专家私教引擎 v23.5

小学数学高密度训练单页应用：基于遗忘曲线的智能出题 + 永久错题本 + GitHub Gist 多设备同步。

## 在线访问

**https://szkailorik.github.io/math-mini-challenge/**

## 多设备同步设置（一次，30 秒）

1. 打开 https://github.com/settings/tokens/new?scopes=gist&description=math-mini-challenge （已预勾 gist 权限）
2. Expiration 选 **No expiration**，点底部 **Generate token**
3. 复制 token（形如 `ghp_xxxx...`）
4. 在应用右上角控制面板点 **💾 数据备份** → 粘贴 token → **🔗 连接云同步**
5. 换设备（iPad/电脑/其他浏览器）时粘贴同一 token → 数据自动合并同步

连接云同步后，数据存在你自己 GitHub 账号下一个**私有 gist** 里（描述为 `math-mini-challenge-sync-v1`）。GitHub Pages 只是网页入口，不保存学习记录。随时可在 https://gist.github.com 查看/删除/导出。不走任何第三方服务器，Anthropic / 其他云服务商看不到你的数据。

## v22.0 重点：错题本永久存档版

相比 v21.0，重写了错题本存储与交互：

| 维度 | v21 旧版 | v22 新版 |
| --- | --- | --- |
| 存储位置 | 挂在 `history[]` 的 session details 里 | 独立 `errorBook{}` 字典，单独持久化 |
| 超 60 次训练后 | `history.shift()` 会裁剪掉老错题 | errorBook 不裁剪，永久保留 |
| 同一题错多次 | 重复出现多张卡片 | 自动去重，合并为一条 + `count` 计数 |
| 重新提交同 set | 错题会被覆盖/丢失 | 可回滚的增量计数，历史错次不丢 |
| 已掌握的题 | 无法从错题本移除 | ✓ 按钮标记掌握，单独归档；再次错→自动撤销 |
| 知识点视图 | 时间线混排 | 按 KnowledgeBase tag 自动分组，组内按错次排序 |
| 数据迁移 | — | 从旧版 `history` 自动重建 errorBook |

核心错题唯一 ID：`tag + FNV-1a(normalized(q))`，不同题不会碰撞，同题跨 set 稳定命中。

## v23.5 打印与题型覆盖优化

- 修复 `打印AB四页` 时部分浏览器会把四张题卷打成八页、其中夹杂空白页的问题。
- 打印模式不再依赖短定时器清理，而是改为 `afterprint` + print media 状态回收，避免预览尚未完成就恢复页面。
- 打印样式改成“打印时最小页高 + 显式可见页分页”，降低固定 297mm 高度和全局分页叠加造成的空白页风险。
- Lorik 的 B 卷第二部分除法现在固定覆盖三类小数练习：被除数带小数点、除数带小数点、被除数和除数都带小数点。
- 运行时验证新增 Lorik 小数除法覆盖检查，以及打印 helper 生命周期检查。

## v23.4 本套批改报告

- 控制面板新增 `本套批改报告 / Set Review`，可集中查看当前 set 的批改结果。
- 每次提交 KAI 或 Lorik 批改后，会自动弹出本套报告。
- 报告按学生列出错题的分区、纸面题号、错误类型、原题、正确答案和复盘提醒，方便和纸质卷子逐题对照。
- 没有提交的学生会显示“还没有提交这套卷子的批改记录”，避免误以为数据丢失。
- 运行时验证新增本套报告检查，确保题号、原题和答案能正确显示。

## v23.3 云端连接引导

- 本地模式时，控制面板会直接显示 `连接云同步 / Connect Sync` 快捷按钮。
- 云同步状态文字可点击，会打开数据备份与 Gist 连接面板。
- 未连接时明确提示“此设备尚未连接云端”，避免误以为 GitHub Pages 会自动保存数据。

## v23.2 跨设备自动同步增强

- 明确数据位置：未连接云同步时只在当前浏览器 localStorage；连接后同步到用户 GitHub 账号下的私有 gist。
- 启动网页时自动拉取云端并与本地合并，避免换设备打开后看不到旧记录。
- 提交批改、标记掌握、导入恢复后仍会自动推送云端。
- 切回页面或页面保持打开时会自动轻量拉取，减少两台设备同时使用时的数据滞后。
- 云同步状态同时显示最近推送和最近拉取时间。

## v23.1 打印四页优化

- 控制面板新增 `打印AB四页` 按钮，点击后只把每天要做的 KAI/Lorik AB 卷四页送进浏览器打印。
- 打印模式会自动隐藏答案页、错题本页、控制面板和页面阴影，避免长页面打印时误打答案或多打空白页。
- 四张试卷标记为 `question-sheet`，第 4 页标记为 `print-last-question`，保证 A4 物理分页稳定。
- 另有 `打印答案` 按钮，方便需要单独打印答案页时使用。
- 运行时验证新增“四张可打印题卷”检查，防止后续排版改动破坏每日四页打印流。

## v23.0 错题联合训练

- 错题本正式接入日常训练卷：活跃错题会通过“原题回炉、同类变式、同领域迁移”三段方式回到训练中。
- 同一题错 2 次以上、或处于遗忘拦截阶段时，更容易触发原题回炉；每个 section 仍限制回炉数量，避免整张卷变成死刷错题。
- 同 tag 有错题但不适合原题重复时，会生成同知识点变式题，并用 `同类变式 / Variant Practice` 标记。
- 知识点地图新增“错题联合训练计划”，展示每个活跃错题将以什么方式进入训练、优先级如何。
- 试卷缓存升级到 `MathSetData_v30`，验证脚本新增错题回炉闭环检查。

## v22.9 领域自适应增强

- 出题算法新增领域级弱项信号：同一领域内的错题和权重会为相关新题提供适度加权。
- 答案页新增 `领域补强 / Domain Boost` 标记，说明这道题是为了修复某个知识领域的整体薄弱。
- 知识领域画像新增“稳定维护 / 适度加练 / 优先补强”优先级，以及活跃错次、已攻克数量。
- 试卷缓存升级到 `MathSetData_v29`，确保领域自适应评分进入新生成的 set。
- 验证脚本新增领域信号计算检查，避免知识画像只展示、不参与训练。

## v22.8 知识水准画像

- 每张训练卷新增阶段目标条，显示本轮训练目标、目标水准和检索练习/间隔/交错原则。
- 出题算法加入 L4 挑战比例控制：挑战日可以提高迁移题比例，普通日避免挑战题挤压基础巩固。
- 知识点地图升级为领域画像，按小数与位值、整数乘除与退位、分数与互化、方程与未知数、简算与综合策略汇总覆盖与弱项。
- 当前弱项表新增领域和 L1-L4 水准，方便把“错题”转成可执行的训练重点。
- 试卷缓存升级到 `MathSetData_v28`，验证脚本同步检查阶段目标条、水准标签和知识领域覆盖。

## v22.7 水准分层增强

- 出题算法加入 L1-L4 水准分层：基础、熟练、易错、挑战。
- 每个 7 套周期会按训练阶段偏向不同水准，同时保留弱项权重和遗忘曲线优先级。
- 题目和答案页都会显示水准标签，方便控制每日训练强度。
- 新增“知识点地图”，按知识族展示训练覆盖面，并标出当前活跃弱项。
- 验证脚本新增水准合法性检查，确保每道生成题都有 1-4 级训练等级。

## v22.6 反馈质量增强

- 答案页每道题新增 `[易错点 Tip]`，直接显示对应知识点和防错建议，方便批改后即时复盘。
- 新增题型家族级建议：精确标签优先，同类标签自动使用分数、互化、小数乘除、方程、简算等家族提示。
- 错题本分组与解析页也统一使用同一套知识点建议，减少无针对性的泛化反馈。
- 验证脚本现在要求每个生成题型都能拿到非泛化的知识点建议。

## v22.5 诊断训练增强

- KAI 增加两步方程、逆向分配律、单位分数应用题，强化“先逆运算、再处理乘除”和“先找单位量”的步骤意识。
- Lorik 增加括号优先级混合运算，专门拦截从左到右顺算的常见错误。
- 试卷缓存升级到 `MathSetData_v25`，确保新题型上线后能进入新生成的 set。
- 运行时验证扩展到 Set 73-102，并检查 section 数量、空题/空答案、重复题和异常字符串。

## v22.4 题型质量增强

- KAI 新增极小数乘法、小数商、未知数作除数、整百万连续借位、补偿凑整等诊断题型。
- Lorik 新增接近整百乘法、25/末尾0除法、整万连续借位、求一个数的几分之几等题型。
- 关键基础题补充解析步骤，让答案页不只给结果，也能提示孩子检查位值、估算和凑整策略。
- 互化题修正小数答案格式，统一用稳定格式输出，减少 `0.30000000004` 这类浮点显示风险。
- 新增 `scripts/validate-runtime.mjs`，可在 Node 中生成整套试卷做轻量运行时验证。

## v22.3 稳定性优化

- 修正 `k_ddiv_basic` 小数除法生成器：被除数保留 3 位，避免四舍五入后题面与答案不一致。
- 加固分数引擎：构造 `Frac` 时先把小数转换为整数比，降低浮点误差导致的异常约分风险。
- 重复提交同一套题时会回滚旧权重、应用新权重，让自适应弱点画像保持一致。
- 本地存储写入失败时会清理旧试卷快照并重试，减少 localStorage 配额满导致的保存失败。
- 避免 Lorik 异分母相减生成负数答案。
- 图片导出组件未加载完成时给出明确提示，而不是静默报错。

## v22.0-v22.2 优化

- `Frac` 构造函数：加固 `n=0` / `d=0` 边界，避免 NaN 污染
- `localStorage` 超配额：快照写入失败时自动清理其他版本缓存后重试
- 数据导入：旧版备份 (v21 及更早) 自动通过 `migrateProfile` 重建 errorBook
- 备份文件名：加日期后缀 `_2026-04-16.json` 方便版本管理
- 答题行识别：使用 `uid` 精确匹配，解决同 tag 不同题的状态串扰

## 数据结构

```
profile = {
  weights:   { tag: number },        // 掌握度加权
  lastSeen:  { tag: setNum },        // 遗忘曲线
  history:   [ {
    set, date, ts,
    details:[{tag, grade, info, uid}],         // 错题明细
    allGrades:[{tag, grade}],                  // 本套完整批改摘要
    weightAdjustments:[{tag, delta}]           // 重交批改时用于回滚画像
  } ],  // 保留 60 套
  errorBook: {                        // ★ 永久，不裁剪
    [uid]: {
      tag, info: {q,a,step,sec},
      grade: 'careless' | 'wrong',
      count: N,
      firstSet, firstDate,
      lastSet, lastDate,
      mastered: false,
      masteredDate
    }
  }
}
```

## 本地运行

单文件纯前端，无构建：

```bash
# 任选其一
python3 -m http.server 8080
npx serve .
```

浏览器访问 `http://localhost:8080/`。

## 本地验证

```bash
sed -n '/<script type="module">/,/<\/script>/p' index.html | sed '1d;$d' > /tmp/math-mini-challenge-script.mjs
node --check /tmp/math-mini-challenge-script.mjs
node scripts/validate-runtime.mjs
```

## 部署

仓库根目录的 `index.html` 直接作为 GitHub Pages 首页。workflow 在 `.github/workflows/pages.yml` 自动发布 `main` 分支的根目录。

## 浏览器要求

- Chrome / Edge / Safari 最近两个大版本
- 需要 ES Modules 支持
- 依赖 `localStorage`；云端同步使用 GitHub Gist API，需要用户提供具备 `gist` scope 的 GitHub PAT
- 图片导出依赖 CDN 加载 `html2canvas`

## 数据安全

- 默认仅本地存储（localStorage）
- 跨设备同步：GitHub Gist（PAT 存本地，不发到任何第三方服务器）
- 冲突处理：多端并发修改时按 uid/ts 安全合并，不会丢数据
- 本地始终是主副本，断网也能用，恢复网络后自动 debounce 回推
- 数据保险箱：JSON 导出/导入作为终极离线备份
